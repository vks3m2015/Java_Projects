package programs;

//Given an array with both +ive and -ive integers, return a pair with highest product
public class MaxProduct {

	/*
	public static void main(String[] args) {
		
		int[] arr = {1, 4, 3, 6, 7, 0}; 
		
		int maxPos, secMaxPos, minNeg, secMinNeg;
		
		for(int i=0; i< arr.length; i++)
		{
			if(arr[i] > maxPos)
			{
				secMaxPos = maxPos;
				maxPos = arr[i];
			}
			else if(arr[i] > secMaxPos)
			{
				secMaxPos = arr[i];
			}
			else if(arr[i] < minNeg)
			{
				secMinNeg = minNeg;
				minNeg = arr[i]; 
			}
			else if(arr[i] < secMinNeg) 
			{
				secMinNeg = arr[i];
			}	
		}	
	}*/

}
