package inheritance;

public class Note {

/*
 *A subclass inherits all of the public and protected members of its parent, no matter what package the subclass 
 *is in. If the subclass is in the same package as its parent, it also inherits the package-private members of
 * the parent. You can use the inherited members as is, replace them, hide them, or supplement them with new members
 * 
 * A subclass does not inherit the private members of its parent class. However, if the superclass has public or 
 * protected methods for accessing its private fields, these can also be used by the subclass
*/
	
	
	
	
	
	
	
	public static void main(String[] args) {
		
	}

}
