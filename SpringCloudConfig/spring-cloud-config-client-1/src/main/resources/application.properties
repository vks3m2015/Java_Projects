spring.application.name=spring-cloud-config-client-1
server.port=8889


# will fail if config server is not up and client is not able to connect config server. But will not fail if we use optional
spring.config.import=configserver:http://localhost:8888

#spring.config.import=optional:configserver:http://localhost:8888

#app.message=This is message in client's property file